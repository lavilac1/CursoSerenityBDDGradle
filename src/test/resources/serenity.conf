webdriver {
  driver = "firefox"
}

serenity {
  take.screenshots = BEFORE_AND_AFTER_EACH_STEP
  driver.capabilities.manual = true
  serenity.test.root = mystore
}
rest {
  serenity.rest.useLogs = true
  serenity.rest.logging = true
  default.url = "https://jsonplaceholder.typicode.com"
}
